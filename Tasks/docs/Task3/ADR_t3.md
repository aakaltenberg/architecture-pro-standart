### <a name="_b7urdng99y53"></a>**Название задачи:** MVP Архитектура онлайн-открытия депозитов для банка «Стандарт»
### <a name="_hjk0fkfyohdk"></a>**Автор:** Кальтенберг А.А.
### <a name="_uanumrh8zrui"></a>**Дата:** 10.04.2026
### <a name="_3bfxc9a45514"></a>**Функциональные требования**

|**№**|**Действующие лица или системы**|**Use Case**|**Описание**|
| :-: | :- | :- | :- |
| 1 | Клиент, Сайт | Просмотр списка депозитов на сайте | Клиент заходит на сайт банка, видит актуальные ставки по депозитам, полученные из централизованного источника. |
| 2 | Клиент, Сайт, Система кол-центра | Подача заявки на депозит с сайта | Клиент заполняет форму с ФИО и номером телефона. Заявка передаётся в систему кол-центра для последующего звонка менеджера. |
| 3 | Клиент, Менеджер кол-центра, Система кол-центра | Звонок менеджера кол-центра для подтверждения заявки | Менеджер кол-центра подтверждает с клиентом заявку. Заявка переходит в статус подтвержденной |
| 4 | Система кол-центра, АБС | Перенаправление заявки на обработку | Заявки со статусом "подтверждена" отправляются в АБС |
| 5 | Клиент, Интернет-банк | Просмотр персонализированных ставок в интернет-банке | Авторизованный клиент видит список депозитов с персональными ставками, рассчитанными на основе его профиля и текущего кредитного риска банка. |
| 6 | Клиент, Интернет-банк, СМС-шлюз | Подача заявки на депозит из интернет-банка | Клиент выбирает депозит, указывает счёт и сумму, подтверждает операцию СМС-кодом. Заявка регистрируется в АБС. |
| 7 | Сотрудник бэк-офиса (депозиты), АБС, СМС-шлюз | Обработка заявки на депозит | Менеджер видит заявку в АБС, подтверждает или корректирует ставку. После подтверждения клиент получает СМС о согласованной ставке и об открытии депозита. |

### <a name="_u8xz25hbrgql"></a>**Нефункциональные требования**

|**№**|**Требование**|
| :-: | :- |
| 1 | Доступность сервисов интернет-банка — 99.9% (24/7), с переключением на резервный ЦОД при сбое. |
| 2 | Горизонтальное масштабирование новых компонентов для выдерживания роста нагрузки. |
| 3 | АБС не должна быть перегружена прямыми онлайн-запросами; использовать промежуточные сервисы, очереди, кэш. |
| 4 | Circuit Breaker для вызовов внешних систем (СМС-шлюз, в будущем – БКИ, скоринг). |
| 5 | Время отклика интерфейса – миллисекунды; быстрая загрузка справочных данных (кэширование). |
| 6 | Использовать преимущественно существующие технологии банка: MS SQL, Oracle, Java, Python, .NET. |
| 7 | Избегать доработок ядра интернет-банка и системы кол-центра силами подрядчика. |
| 8 | Чувствительные данные передавать только по HTTPS/TLS, хранить персональные данные в зашифрованном виде. |
| 9 | Запрет прямого доступа интернет-банка к БД АБС для нового функционала. |

### <a name="_qmphm5d6rvi3"></a>**Решение**
1. В рамках архитектурного решения была предложена интеграция между сайтом и системой бэкэнд кол-центра. Интеграция предлагается с помощью Rest API на базе текущего бэкенд-приложения кол-центра. У команды банка есть опыт работы с Java, поэтому данную работу можно будет выполнить без привлечения подрядчика. Сервис является горизонтально масштабируемым, поэтому проблем при возрастающей нагрузке быть так не должно.

2. В рамках архитектурного решения была предложена интеграция между кол-центром и системой АБС. Ранее интеграция между двумя этими системами уже была, но не было указано каким образом было реализовано это взаимодействие (вероятно, DB to DB, либо API, который дальше шел в Oracle АБС). В текущей реализации предлагается на стороне кол-центра реализовать сервис, который будет передавать сообщения в Kafka, развернутую на стороне АБС (либо поток, проходящий через ранее реализованый API уже на стороне АБС, перенаправить в кафку)

3. В рамках архитектурного решения была предложена интеграция между интернет-банком и системой АБС. Интеграция выбрана асинхронная, интернет-банк создает сообщения, передает их в кафку, развернутую в АБС.

4. В рамках архитектурного решения было предложено сохранять все необходимые справочники в локальное хранилище интернет-банка MS SQL, а также хранить самые горячие справочные данные в кэше InProcess сервиса депозитных заявок. Обновления локального хранилища производить раз в какой-то период.

```puml
@startuml
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Context.puml

Person(customer, "Клиент", "Посетитель сайта / Пользователь интернет-банка")
Person(cc_manager, "Менеджер кол-центра", "Подтверждает заявки с сайта")
Person(backoffice, "Сотрудник бэк-офиса депозитов", "Обрабатывает заявки на депозиты")

System(site, "Сайт банка", "PHP/React")
System(ib, "Интернет-банк", "ASP.NET MVC")
System(cc_system, "Система кол-центра", "Java/React")
System(abs, "АБС", "Delphi/Oracle")
System_Ext(sms, "СМС-шлюз", "Отправка уведомлений")

Rel(customer, site, "Просматривает депозиты, подаёт заявку", "HTTPS")
Rel(site, cc_system, "Передаёт заявку с контактами", "REST API")
Rel(cc_manager, cc_system, "Просматривает заявки, подтверждает", "Веб-интерфейс")
Rel(cc_system, abs, "Передаёт подтверждённую заявку", "Очередь / REST API")
Rel(customer, ib, "Просматривает персональные ставки, подаёт заявку с СМС", "HTTPS")
Rel(ib, abs, "Регистрирует заявку из интернет-банка", "Очередь / REST API через сервис")
Rel(backoffice, abs, "Обрабатывает заявки, подтверждает ставки", "Десктоп-клиент")
Rel(abs, sms, "Отправляет СМС о статусе заявки", "HTTP/SMPP")
@enduml
```

```puml
@startuml
!includeurl https://raw.githubusercontent.com/RicardoNiepel/C4-PlantUML/master/C4_Container.puml

Person(customer, "Клиент")
Person(cc_manager, "Менеджер кол-центра")
Person(backoffice, "Сотрудник бэк-офиса")

System_Boundary(ib_system, "Интернет-банк") {
    Container(web_app, "Веб-приложение", "ASP.NET MVC 4.5", "Пользовательский интерфейс, формы заявок")
    Container(deposit_service, "Сервис депозитных заявок", "Java Spring Boot", "Приём заявок, расчёт персональных ставок, отправка в АБС")
    ContainerDb(deposit_db, "БД депозитного сервиса", "MS SQL", "Хранение заявок, справочников ставок")
}

System_Boundary(cc_system_boundary, "Система кол-центра") {
    Container(cc_ui, "Веб-интерфейс кол-центра", "React", "Рабочее место менеджера")
    Container(cc_backend, "Бэкенд кол-центра", "Java Spring Boot", "Управление обращениями")
    ContainerDb(cc_db, "БД кол-центра", "PostgreSQL", "Хранение заявок и обращений")
}

System_Boundary(abs_system, "АБС") {
    ContainerDb(abs_db, "Основная БД", "Oracle", "Учёт счетов, депозитных договоров")
    Container(abs_ui, "Десктоп-клиент", "Delphi", "Рабочее место бэк-офиса")
    Container(abs_gateway, "Шлюз для онлайн-сервисов", "PL/SQL API / Kafka Consumer", "Точка входа для заявок из внешних систем")
}

System_Ext(sms_gw, "СМС-шлюз")
System_Ext(site, "Сайт банка", "PHP/React")

Rel(customer, site, "Подаёт заявку", "HTTPS")
Rel(site, cc_backend, "Передаёт заявку", "REST API")

Rel(cc_manager, cc_ui, "Подтверждает заявки", "HTTPS")
Rel(cc_ui, cc_backend, "API вызовы", "REST")
Rel(cc_backend, cc_db, "Сохраняет статусы", "JDBC")
Rel(cc_backend, abs_gateway, "Отправляет подтверждённую заявку", "Kafka")

Rel(customer, web_app, "Использует", "HTTPS")
Rel(web_app, deposit_service, "Вызывает API", "REST (JSON)")
Rel(deposit_service, deposit_db, "Сохраняет заявки, кэш ставок", "JDBC")
Rel(deposit_service, abs_gateway, "Отправляет заявку", "Kafka")

Rel(abs_gateway, abs_db, "Вставляет заявку", "PL/SQL")
Rel(backoffice, abs_ui, "Обрабатывает заявки", "")
Rel(abs_ui, abs_db, "Чтение/запись", "PL/SQL")
Rel(abs_db, sms_gw, "Инициирует отправку СМС", "HTTP")
@enduml
```

### <a name="_bjrr7veeh80c"></a>**Альтернативы**
1. В качестве альтернативного решения в дальнейшем для кэширования справочников можно перейти на использование DistributedMemory - Redis. Сейчас команды не знакомы с данной технологией.
2. Использование REST API напрямую из интернет-банка в АБС	АБС не имеет публичного API, способного выдержать пиковые нагрузки. Команда АБС предупредила о риске деградации.
3. Полный отказ от Kafka и использование БД как очереди	Приведёт к дополнительной нагрузке на Oracle и усложнит масштабирование.

**Недостатки, ограничения, риски**

1. Дополнительная сложность из-за введения нового сервиса и Kafka. Отсутствие у команды компетенций по Kafka.
2. Задержка между подачей заявки и её появлением в АБС (асинхронность)
3. Необходимость доработки АБС для чтения из Kafka
4. Вертикальное масштабирование АБС остаётся узким местом

