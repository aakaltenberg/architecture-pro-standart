### <a name="_b7urdng99y53"></a>**Название задачи:** MVP Архитектура онлайн-подачи заявки на кредит для банка «Стандарт»
### <a name="_hjk0fkfyohdk"></a>**Автор:** Кальтенберг А.А.
### <a name="_uanumrh8zrui"></a>**Дата:** 11.04.2026
### <a name="_3bfxc9a45514"></a>**Функциональные требования**

|**№**|**Действующие лица или системы**|**Use Case**|**Описание**|
| :-: | :- | :- | :- |
| 1 | Клиент, Сайт, Кредитный конвейер | Просмотр кредитных предложений на сайте | Клиент заходит на сайт банка, видит актуальные условия по кредитам, полученные из Кредитного конвейера. |
| 2 | Клиент, Сайт, Система скоринга, БКИ | Подача заявки на кредит с сайта с мгновенным решением | Клиент заполняет форму с ФИО, телефоном и паспортными данными. Система вызывает скоринг, используя данные БКИ, и сразу показывает предварительное решение (одобрено/отказано). |
| 3 | Клиент, СМС-шлюз | Получение СМС о статусе заявки с сайта | После подачи заявки клиент получает СМС с уведомлением о необходимости посетить отделение для идентификации и завершения оформления. |
| 4 | Клиент, Интернет-банк | Просмотр предодобренных кредитных предложений | Авторизованный клиент в интернет-банке видит персональные предодобренные предложения по кредитам, основанные на предварительном скоринге. |
| 5 | Клиент, Интернет-банк, СМС-шлюз | Подача заявки на кредит из интернет-банка | Клиент выбирает предложение, указывает счёт зачисления и подтверждает операцию СМС-кодом. Заявка регистрируется, инициируется повторный скоринг (упрощённый). |
| 6 | Сотрудник отделения, АБС, Кредитный сервис | Просмотр предварительно одобренной заявки в отделении | Менеджер в отделении через АБС видит заявку клиента, созданную онлайн, и продолжает её обработку (идентификация, подписание документов). |
| 7 | Сотрудник бэк-офиса (кредиты), Кредитный конвейер | Обработка заявки на кредит | Для обычных заявок (не предодобренных) сотрудник бэк-офиса работает в Кредитном конвейере, анализирует данные скоринга и принимает решение. |
| 8 | Клиент, СМС-шлюз | Информирование об изменении статуса заявки | Клиент получает СМС-уведомления при изменении статуса кредитной заявки (например, одобрена, требуется визит, выдана). |

### <a name="_u8xz25hbrgql"></a>**Нефункциональные требования**

|**№**|**Требование**|
| :-: | :- |
| 1 | Доступность сервиса кредитных заявок — 99.9% (24/7), с переключением на резервный ЦОД. |
| 2 | Горизонтальное масштабирование нового сервиса кредитных заявок для выдерживания пиковых нагрузок. |
| 3 | Запрет прямого доступа интернет-банка и сайта к БД АБС; использование промежуточных сервисов и очередей. |
| 4 | Circuit Breaker для вызовов внешних систем (скоринг, БКИ, СМС-шлюз). |
| 5 | Время отклика интерфейса при подаче заявки — не более 2 секунд; для предодобренных предложений — миллисекунды. |
| 6 | Использовать существующие технологии банка: Java, Python, MS SQL, Oracle. |
| 7 | Избегать доработок ядра интернет-банка и Кредитного конвейера силами подрядчика. |
| 8 | Чувствительные данные (паспорт, ФИО) передавать только по HTTPS/TLS и хранить в зашифрованном виде. |
| 9 | Система скоринга не должна нагружаться дополнительными предрасчётами; предодобренные предложения формируются асинхронно в периоды низкой нагрузки. |
| 10 | Обход ограничения пакетной передачи АБС ↔ Кредитный конвейер для ускорения обработки онлайн-заявок. |

### <a name="_qmphm5d6rvi3"></a>**Решение**

1. Выделение отдельного сервиса кредитных заявок (credit-service). Аналогично депозитному сервису, это позволяет изолировать новую функциональность от монолитных систем (интернет-банк, АБС) и масштабировать её независимо. Сервис реализуется на Java Spring Boot, используя существующую экспертизу команды (Java-стек уже применяется в системе кол-центра и кредитном конвейере)
2. Прямая интеграция с системой скоринга и БКИ. Для мгновенного решения по заявке с сайта необходимо выполнить скоринг в реальном времени. Система скоринга предоставляет REST API, что позволяет быстро интегрироваться. Предодобренные предложения для интернет-банка рассчитываются асинхронно (например, ночью) и сохраняются в кэше (Redis), чтобы не нагружать скоринг в пиковые часы.
3. Обход ограничения передачи АБС ↔ Кредитный конвейер раз в сутки. Существующая интеграция между АБС и конвейером работает раз в сутки и не может быть ускорена. Поэтому онлайн-заявки не должны проходить через этот канал на этапе подачи. Вместо этого сервис кредитных заявок напрямую вызывает API скоринга и, при необходимости, отправляет заявку в Кредитный конвейер для ручной обработки (если решение не может быть принято автоматически). Для стандартных заявок бэк-офис продолжает работать в конвейере. Для отображения заявки менеджеру в отделении сервис асинхронно отправляет данные в АБС через шлюз (Kafka + PL/SQL). Это гарантирует появление заявки в АБС в течение нескольких секунд, что достаточно для визита клиента.
4. Интеграция с интернет-банком и сайтом. Сайт и интернет-банк обращаются к сервису кредитных заявок по REST API. Это позволяет не вносить существенных изменений в их ядро (только добавить новые эндпоинты). Для подтверждения операций используется существующий СМС-шлюз, вызов которого инициирует сервис кредитных заявок.
5. Хранение предодобренных предложений. Для быстрого отображения в интернет-банке используется Distributed-кэш Redis. Периодический расчёт выполняется отдельным потоком в сервисе в часы низкой нагрузки, что защищает систему скоринга от перегрузки.
6. Отказоустойчивость и масштабирование. Сервис кредитных заявок выделен в отдельный сервис, который может быть горизонтально масштабируем. Для взаимодействия с внешними системами (скоринг, БКИ) применяется паттерн Circuit Breaker, чтобы изолировать сбои.


```puml
@startuml title DepositService-CallServiceIntegration Context Diagram

!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Context.puml

Person(customer, "Клиент", "Подаёт заявку на кредит онлайн")
Person(front_manager, "Сотрудник отделения", "Работает с заявкой в АБС")
Person(back_manager, "Сотрудник бэк-офиса кредитов", "Обрабатывает заявки в Кредитном конвейере")

System(site, "Сайт банка", "PHP/React")
System(ib, "Интернет-банк", "ASP.NET MVC")
System(credit_service, "Сервис кредитных заявок", "Java Spring Boot")
System(abs, "АБС", "Delphi/Oracle")
System(conveyor, "Кредитный конвейер", "Camunda/Java")
System(scoring, "Система кредитного скоринга", "Python/Flask")
System_Ext(bki, "Бюро кредитных историй", "Внешняя система")
System_Ext(sms, "СМС-шлюз", "Отправка уведомлений")

Rel(customer, site, "Просмотр предложений, подача заявки", "HTTPS")
Rel(site, credit_service, "Отправка заявки", "REST API")
Rel(customer, ib, "Просмотр предодобренных предложений, подача заявки", "HTTPS")
Rel(ib, credit_service, "Получение предложений, отправка заявки", "REST API")
Rel(credit_service, scoring, "Запрос скоринга", "REST API")
Rel(scoring, bki, "Получение кредитной истории", "REST API")
Rel(credit_service, conveyor, "Передача заявки для бэк-офиса", "REST API (или Kafka)")
Rel(credit_service, abs, "Регистрация заявки для отделения", "Kafka")
Rel(front_manager, abs, "Просмотр заявки", "Десктоп-клиент")
Rel(back_manager, conveyor, "Обработка заявки", "Веб-интерфейс")
Rel(credit_service, sms, "Отправка СМС", "HTTP")
Rel(conveyor, sms, "Отправка СМС", "HTTP")

@enduml
```


```puml
@startuml

!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Container.puml

Person(customer, "Клиент")
Person(front_manager, "Сотрудник отделения")
Person(back_manager, "Сотрудник бэк-офиса кредитов")

System_Boundary(site_boundary, "Сайт банка") {
    Container(site_ui, "Веб-сайт", "PHP/React", "Публичные страницы кредитов, форма заявки")
}

System_Boundary(ib_boundary, "Интернет-банк") {
    Container(ib_ui, "Веб-приложение", "ASP.NET MVC", "Личный кабинет, предодобренные предложения")
}

System_Boundary(credit_service_boundary, "Сервис кредитных заявок") {
    Container(credit_api, "API кредитных заявок", "Java Spring Boot", "Приём заявок с сайта и интернет-банка")
    Container(credit_db, "БД кредитных заявок", "MS SQL", "Хранение заявок, статусов, решений")
    Container(offer_cache, "Кэш предодобренных предложений", "Redis", "Хранение рассчитанных оффлайн предложений")
}

System_Boundary(abs_boundary, "АБС") {
    ContainerDb(abs_db, "Основная БД", "Oracle", "Учёт счетов, кредитных договоров")
    Container(abs_ui, "Десктоп-клиент", "Delphi", "Рабочее место менеджера отделения")
    Container(abs_gateway, "Шлюз для онлайн-сервисов", "Kafka Consumer / PL/SQL", "Приём заявок из внешних систем")
}

System_Ext(conveyor_system, "Кредитный конвейер", "Camunda + Oracle")
System_Ext(scoring, "Система скоринга", "Python/Flask + PostgreSQL")
System_Ext(bki, "Бюро кредитных историй", "Внешний REST API")
System_Ext(sms, "СМС-шлюз")

Rel(customer, site_ui, "Подача заявки", "HTTPS")
Rel(site_ui, credit_api, "Отправка данных заявки", "REST (JSON)")
Rel(customer, ib_ui, "Просмотр предложений, подача заявки", "HTTPS")
Rel(ib_ui, credit_api, "Запрос предложений, отправка заявки", "REST (JSON)")

Rel(credit_api, scoring, "Запрос скоринга", "REST (JSON)")
Rel(scoring, bki, "Получение КИ", "REST (SOAP)")
Rel(credit_api, credit_db, "Сохранение заявки", "JDBC")
Rel(credit_api, offer_cache, "Чтение/запись предложений", "Redis Protocol")
Rel(credit_api, conveyor_system, "Отправка заявки в бэк-офис (если требуется ручная обработка)", "REST / Kafka")
Rel(credit_api, abs_gateway, "Регистрация заявки для отделения", "Kafka")

Rel(front_manager, abs_ui, "Просмотр заявки клиента", "Прямой доступ к БД")
Rel(abs_ui, abs_db, "Чтение/запись", "Oracle Net")
Rel(abs_gateway, abs_db, "Вставка заявки", "PL/SQL")

Rel(back_manager, conveyor_system, "Обработка заявок", "HTTPS")
Rel(conveyor_system, scoring, "Повторный скоринг при необходимости", "REST")

Rel(credit_api, sms, "Отправка уведомлений", "HTTP")
Rel(conveyor_system, sms, "Отправка уведомлений", "HTTP")

@enduml
```

### <a name="_bjrr7veeh80c"></a>**Альтернативы**
1. Реализовать логику кредитных заявок внутри интернет-банка. Привело бы к разрастанию монолита, зависимости от подрядчика, сложностям с масштабированием и нарушению запрета прямого доступа к БД АБС.
2. Использовать существующую интеграцию АБС → Кредитный конвейер для онлайн-заявок. Невозможно из-за ограничения на передачу раз в сутки, что не соответствует требованию обработки в течение дня.
3. Передавать заявки напрямую в Кредитный конвейер, минуя сервис кредитных заявок. Конвейер оптимизирован для внутренней обработки бэк-офисом, не имеет публичного API с нужным уровнем безопасности и не рассчитан на прямой вызов из интернет-банка. Сервис выступает защитным слоем.
4. Хранить предодобренные предложения в скоринге. Система скоринга уже испытывает пиковые нагрузки, добавление хранилища предложений усложнит её и увеличит нагрузку.
5. Использовать вместо Redis локальное хранение в отдельной таблице/схеме MS SQL. Redis быстрее.
6. Отказаться от асинхронной отправки в АБС и делать синхронный REST-вызов. АБС не готова к синхронной высокой нагрузке; асинхронный подход через Kafka буферизует запросы и защищает АБС.


**Недостатки, ограничения, риски**

1. Дополнительная сложность из-за нового сервиса и интеграций. 
2. Необходимость доработки АБС для приёма заявок через шлюз (аналогичные доработки запланированы в рамках онлайн-депозитов).
3. Задержка при отображении заявки в АБС (асинхронность). Для MVP допустима задержка в несколько секунд. В будущем можно рассмотреть синхронный API с жёстким rate limiting.
4. Отсутствие у команды опыта работы с Redis. 
5. Сложность синхронизации статусов заявки между сервисом, конвейером и АБС. Решается хорошей проработкой state-machine.
